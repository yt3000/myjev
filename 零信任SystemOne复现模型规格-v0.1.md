## Jev 式 System One 决策模型 · 复现规格（零信任安全路由场景）

版本 v0.4（草案，待评审） · 日期 2026-09-21 · 状态：Draft · v0.2：§5.2 按 2026-09 生态复核组件选型 · v0.3：吸收 OpenJev（TheoLeeCJ，WebGPU 单步 Logits 探针）与 ic.work 评测分析——新增免解码探针为 M5 首选形态（§5.2/§5.3）、校准性质红线（§6.1）、扰动防护（§8.5）与探针门禁（§11.2） · v0.4：新增 §13 外部对比基线（Jev / OpenJev 实测 vs 复现目标）及配套可视化图表页

---

## 1. 目标与边界

### 1.1 目标

自托管复现 Jev（TypeSafe System One）的**输出契约与决策机制**，用于零信任安全网关的访问决策：类型化、带校准概率的结构化决策，替代"调用闭源大模型云服务做分类/打分"的方案。

核心要求（与 Jev 对齐的四特性）：

1. 枚举约束输出（无自由文本，结构化 by construction）；
2. 每个决策/信号带**校准后的**概率与置信度；
3. 热路径低延迟（单前向，P50 < 10ms，P99 < 200ms，控制面异步可放宽）；
4. 完全自主：权重、数据、阈值、日志均不出域。

### 1.2 非目标（明确不做）

- 不复现 Jev 对"任意自然语言新问题"的零样本泛化——本模型只覆盖本文档冻结的决策 schema；
- 不做 L3/L4 包级过滤（热通路走 PEP 缓存决策，模型只在控制面）；
- 不承担多步 APT 研判、事件长上下文关联（属 System Two：SIEM/分析师/生成模型冷路径）；
- 模型**无最终裁量权**：硬策略规则优先，模型仅输出建议信号（见 §7 决策层契约）。

### 1.3 术语

| 术语 | 含义 |
|---|---|
| PEP / PDP | 策略执行点（网关/边车） / 策略决策点（本系统 + 规则引擎的合体，PDP 是权威） |
| Choice 头 | 枚举单选输出，返回 {label, probabilities, confidence} |
| Score 头 | 有序刻度打分（连续值 + 档位），可落在档位之间 |
| Noul 头 | 布尔问题，返回"为真的概率" |
| step_up | 升级验证（追加认证因子/降权），介于 allow 与 deny 之间 |

---

## 2. 决策契约（Schema 冻结）

### 2.1 输出结构

```json
{
  "schema_version": "1.0",
  "model": "zt-one-cls",
  "model_version": "0.1.0",
  "decision": {
    "action": "step_up",
    "confidence": 0.87
  },
  "risk": {
    "score": 0.62,
    "band": "medium"
  },
  "auth_required": "mfa_fido2",
  "noul": {
    "credential_stuffing": 0.08,
    "account_takeover": 0.41,
    "device_untrusted": 0.63,
    "impossible_travel": 0.05,
    "bot_automation": 0.52,
    "phishing_target": 0.03,
    "data_exfil_pattern": 0.11,
    "sensitive_policy_hit": 0.22
  },
  "policy_meta": {
    "threshold_set_id": "ts-2026w39",
    "feature_snapshot_id": "fs-0192",
    "fallback_reason": null
  },
  "cache_ttl_s": 300
}
```

### 2.2 枚举定义（不可随意增删，变更走版本升级）

- `decision.action` ∈ {`allow`, `step_up`, `deny`}，softmax 三分类。
- `auth_required` ∈ {`none`, `password_ok`, `mfa_totp`, `mfa_fido2`, `device_cert`, `human_review`}，与 action 联合约束：`deny` 时恒为 `none`（决策层强制，不依赖模型自洽）。
- `risk.band` ∈ {`low`, `medium`, `high`, `critical`}，四档序数；`risk.score` ∈ [0,1] 连续值，band 由 score 双迟滞阈值切分（见 §6.3）。
- `confidence` = 选中 action 的校准概率；若 max prob < 灰区下限（默认 0.60），`action` 强制置 `step_up`、`policy_meta.fallback_reason = "gray_zone"`，可路由 human_review/System Two。

### 2.3 Noul 头清单（v1 冻结 8 项）

| 头 | 问题（自然语言定义，用于标注对齐） | 主标签来源 |
|---|---|---|
| credential_stuffing | 本次认证尝试是否属于自动化撞库/凭证填充 | 事件回溯 + 行为规则标注 |
| account_takeover | 会话操作者特征是否显著偏离该账号历史主体 | 安全事件复盘标签 |
| device_untrusted | 设备姿态是否不满足基线（root/签名失效/策略不合规） | MDM 事实源（弱标签，近真值） |
| impossible_travel | 位置+时间是否构成不可可能旅行 | 确定性规则（近真值） |
| bot_automation | 请求是否呈现自动化/bot 特征 | 人工审计抽样 |
| phishing_target | 访问目标是否匹配钓鱼/恶意基础设施 | 威胁情报 + 人工复核 |
| data_exfil_pattern | 行为是否匹配大批量数据外传特征 | DLP 事件回溯 |
| sensitive_policy_hit | 请求是否命中高敏感策略条款（文本头，吃目标资源描述+请求文本） | 策略工程师标注 |

---

## 3. 输入特征规范

### 3.1 特征组总表

| 组 | 字段（示例） | 类型 | 来源 | 缺失策略 | 敏感 |
|---|---|---|---|---|---|
| S 主体 | user_id 嵌入、角色、历史登录成功率(30d/7d)、账号年龄、近期改密/改绑卡事件 | 类别+数值 | IdP / 目录服务 | 缺失→专用 `<NA>` 桶并置缺失位 | 是（假名化后入模，禁用明文姓名/邮箱） |
| D 设备 | posture 合规布尔、OS/版本、MDM 注册、证书链有效、越狱/root、指纹(哈希) | 类别+布尔 | 设备代理（签名上报，见 §8.3） | 无 posture → `device_untrusted` 先验拉高 | 否 |
| C 会话/行为 | 距上次登录时长、设备切换标志、同 ASN 并发会话数、请求速率、UA 熵、点击/键入节律统计（可选） | 数值 | 网关遥测 | 冷启动账号→群体先验 | 是（行为数据） |
| G 地理/时间 | IP 归属(国家/省)、IP 信誉分、代理/IDC 标志、是否非常规时段 | 类别+数值 | GeoIP / 信誉源 | 默认 unknown | 否 |
| T 目标资源 | 资源敏感度档、数据分类、微分段 zone、所需权限 | 序数+类别 | 资源注册表 | 缺省按 `medium` 保守 | 否 |
| R 请求文本 | request_path、method、UA 原文、目标 URL、（可选）审批工单/异常描述文本 | 文本 ≤256 token | 网关 | 空串 | 是（可能含内部信息，仅本地处理） |
| H 历史窗口 | 同主体 5m/1h/24h 聚合（登录次数、失败次数、异常事件数、风险分 EWMA） | 数值 | 特征存储（Fstore） | 新主体→零向量+标志位 | 是 |

### 3.2 文本特征处理

- R 组与资源描述拼成模板化文本，经小 encoder（§5.2）出 1024 维句向量（可经 MRL 截断至 512 维以压延迟），冻结或微调末两层。
- 模板固定，防止自由文本注入模型内部（预处理规则见 §8.1）。
- 长度上限 256 token，截断标志位入特征。

### 3.3 特征快照（审计硬要求）

每次推理落盘 `feature_snapshot`（fs-id + 列式二进制，保留期 ≥ 370 天），保证任何历史决策可精确回放复现。

---

## 4. 标签体系与数据

### 4.1 标签来源与优先级

1. **PDP 最终执行结果**（规则+模型合成后的 allow/step_up/deny 及人工覆盖）→ `action` 弱监督（注意循环风险，见 §10.3）；
2. **人工复核**（灰区队列 + 抽样审计，双人仲裁）→ 金标，优先用于评测与校准集；
3. **事件复盘回溯**（确认的 ATO/撞库/外传事件，按 fs-id 反查补标）→ Noul 头与 risk 的金标正例；
4. **确定性规则/事实源**（MDM、impossible_travel、TI 匹配）→ 近似真值的弱标签。

### 4.2 risk.score 的构造

无直接真值。以"该请求在人工/复盘视角下应被怎样对待"为锚：`allow→0.1、step_up→0.5、deny→0.9、确认事件→0.95`，加 ±0.05 抖动平滑，作为回归目标；band 阈值在评测集上重标定。

### 4.3 数据集划分（防泄漏硬规定）

- **一律按时间滚动切分**：训练 = 前 70 天，校准 = 中 7 天（专用，不得复用于早停），验证 = 后 7 天，测试 = 最近 7 天；周级滚动重训时同法。
- 同一主体不得跨 split（按 user_hash 分组切分）。
- 负例降采样仅允许用于训练集；校准/验证/测试集保持**真实先验分布**，否则概率失真。

### 4.4 冷启动数据飞轮（M1 前置）

Phase 0（无模型）：纯规则 PDP 上线 + 全量决策日志 + 特征快照开启 → 灰区与抽样人工标注（目标：首月 ≥ 5 万条复核样本，确认事件 ≥ 200 正例，不足则用合成注入样本仅做 smoke test，不入校准集）→ 方可训 v0。

---

## 5. 模型架构

### 5.1 方案 A（主线）：多任务判别式融合模型

```
表格特征 S/D/C/G/T/H ──归一化+类别嵌入──┐
                                          ├─ concat ─ MLP主干(4层,512) ─┬─ head_choice   (3类 softmax)
文本特征 R/资源描述 ──小encoder→1024d(MRL截512)┘                        ├─ head_authreq    (6类 softmax)
                                                                        ├─ head_risk       (回归→CORN序数双输出)
                                                                        └─ heads_noul×8    (sigmoid BCE)
```

- **基线先行**：LightGBM 每头独立跑通（可解释、快上线），transformer+MLP 融合模型必须在验证集上显著超过 GBM 基线才允许替换主线。
- **损失**：`L = λ1·CE(action) + λ2·CE(auth) + λ3·(MSE + CORN序数) + λ4·Σ BCE(noul_i) + λ5·L_mono`
  - `L_mono`：一致性正则——要求 risk.score 与 P(deny)+0.5·P(step_up) 的 Spearman 方向一致（batch 内成对 hinge）。
  - 初始权重 λ = (1, 0.5, 0.5, 0.5, 0.2)，用验证集网格微调。
- **类不均衡**：正例重加权（focal loss 备选，禁止过采样进校准集）。

### 5.2 组件选型清单（v0.2 更新：按 2026-09 生态校准）

| 组件 | 首选 | 备选 | 说明 |
|---|---|---|---|
| 表格基线 | LightGBM（钉版本） | CatBoost | 持续维护，仍是现役；先跑通全链路 |
| 融合主干 | MLP (PyTorch→ONNX) | FT-Transformer | 架构选型，不随版本过时 |
| 文本 encoder | **Qwen3-Embedding-0.6B**（Apache-2.0，1024d，支持 MRL 截断，32K 上下文，显存 ≈1.5GB） | **BGE-M3**（81.5 分性价比标杆，568MB）；GLM-5-Embedding（2026 新，1024d）；bge-small-zh-v1.5 仅作**过渡基线/极限 CPU 兜底** | 2026-06 中文检索实测：原选 bge-small/DeBERTa-v3/mE5 系 2021–2024 旧品，已非小尺寸最优；0.6B 需 INT8 量化 + MRL 降维以满足 §5.3 热路径预算 |
| 服务 | ONNX Runtime (CPU 可) | TensorRT；LLM 侧 vLLM/SGLang | 单副本目标 P50<10ms |
| 校准 | 温度缩放→isotonic | Platt | 方法稳定，每头独立，见 §6 |
| 冷路径 Logits 探针（**M5 首选形态**） | **Qwen3.5-4B**（Q4_K_M ≈3GB；OpenJev 评测在 TypeSafe 公开 102 行子集一致率 84.5%，0.6B/2B 档仅 40.7%/63.7%——零样本探针必须 ≥4B）+ **单步读选项 token logits**：logit_bias 等额约束候选 + softmax，0 token 生成 | 受限解码 JSON（xgrammar/outlines）作对照；服务端 vLLM/Ollama 单步打分接口；端侧 wllama/WebGPU（离线分诊） | OpenJev 实测免解码提速 2.11–2.78×（单决策）、**5.21×**（21 问共享上下文）；代价：概率仅为候选间条件分布，见 §6.1 红线与 §8.5 去偏 |

**版本时效性约定**：模型类组件（encoder、冷路径 LLM）半年级复核一次；库类组件（LightGBM/ONNX Runtime/校准工具）在 M0 契约冻结时统一钉版本并记入模型卡，升级走 §9 版本四元组变更流程。

### 5.3 延迟与部署预算

| 通路 | 场景 | 预算 |
|---|---|---|
| 数据面 | 已缓存决策命中 | < 1ms（查缓存，无模型） |
| 控制面同步 | 新会话/新资源首次决策 | P99 < 200ms |
| 控制面异步 | 持续验证重评分、会话降权 | 秒级 |

单卡/CPU 推理：encoder+MLP 融合模型量化后（INT8 ONNX）目标 CPU 8 核内 P50 < 10ms。

**共享上下文批处理（批量/合规场景）**：同一上下文的多个问题（如批量 Noul 核查、长文档逐条判定）必须复用已编码前缀（KV cache / 一次编码多问），OpenJev 实测 21 问共享上下文时免解码方案达 5.21× 提速；zhihz/openjev 则因"逐问重编码上下文"被点名为待改进项。M5 探针接口设计时列为硬需求，判别式主线天然满足（一次前向出多头）。

---

## 6. 校准与阈值

### 6.1 校准

- 每个概率头独立校准，仅在校准集（§4.3 专用 7 天）上做：先温度缩放，ECE 仍 > 0.02 再升 isotonic。
- 验收：**全部头 ECE ≤ 0.02**，关键头（account_takeover、action）≤ 0.015；可靠性图随模型卡归档。
- 线上每周估计 ECE 代理指标（分桶命中率 vs 标称概率），漂移超 ±0.03 触发重校准。
- **红线（OpenJev 教训）**：对 logits 归一化方案，输出只是"候选 token 间的条件概率"，**不是**统计意义上的置信度——未经本节校准的探针概率**禁止**作为阈值判据/审计证据，仅可用于候选排序。Jev 与社区探针的本质分界正是其 RLCD（强化学习校准决策）训练纠正了概率的系统性偏差；自研探针的对应平替 = 探针输出 + 温度缩放/isotonic 后校准 + §6.2 阈值收紧。

### 6.2 阈值（双阈值 + 灰区）

| 参数 | 默认 | 说明 |
|---|---|---|
| θ_deny | P(deny) > 0.85 → deny | 成本敏感：deny 误伤 = 业务中断 |
| θ_allow | P(allow) > 0.90 且 risk < 0.35 → allow | allow 需同时满足动作与分数双门 |
| 灰区 | 其余 → step_up / human_review | 灰区占比监控见 §10 |
| 迟滞 | band 切换需连续 2 次重评分越界 | 防会话在档位间抖动 |

阈值按"误拦:漏放 = 1:8"的非对称代价（可配置）在验证集上做代价曲线选定，绑定 `threshold_set_id` 版本化。

### 6.3 决策→权限上限联动

`auth_required` 与 `risk.band` 共同决定签发权限上限：`granted = min(资源策略所需, 档位上限表)`。critical 档即使 action=allow 也只给只读+时长 60s（爆炸半径控制，§7.3）。

---

## 7. 与决策层（PDP）的集成契约

### 7.1 权限边界（模型无否决权的红线）

1. 硬规则（denylist、合规红线、离线账号）**优先且模型不可翻转**；
2. 模型可在灰区**提升**验证强度（allow→step_up→deny），**降低**强度仅允许发生在白名单业务 + 低风险档内；
3. allowlist 内部服务/运维 break-glass 通道绕过模型，全程强审计。

### 7.2 接口

- PEP → 模型服务：gRPC，输入 = fs-id + 特征向量（网关侧特征适配器组装），输出 = §2.1 JSON + `cache_ttl_s`；
- 超时 200ms：超时即回退 `step_up + human_review`（**fail-closed on uncertainty**，非 fail-open），`fallback_reason="timeout"` 计数告警。

### 7.3 缓存失效条件

TTL 到期 / 主体上下文变更（角色、设备、IP 大洲变化）/ 全局风险事件（漏洞爆发、TI 命中）/ risk EWMA 越界 → 强制重决策。

---

## 8. 对抗鲁棒与安全

### 8.1 输入防护

- 文本特征进 encoder 前执行：全半角归一、控制符剥离、截断、可疑模式标记位（大量 padding、指令注入短语、编码混淆）——注入检测本身就是特征，禁止原文影响结构字段解析。
- 模板化拼接：请求文本只能落在固定槽位内。

### 8.2 试探规避检测

- 攻击者会向阈值发起"探针"：对同一主体概率输出接近阈值(±0.05)的尝试频率建模，超基线 → 自动降档 + 告警；
- 输出**粗粒化披露**：对 PEP 只暴露 action/band/校准概率；不向调用方回传逐头原始概率，避免梯度式探测。

### 8.3 数据投毒防护

- 设备 posture 由代理签名上报，网关验签，防伪造"干净姿态"；
- 标注流程双人仲裁 + 标注者产量/分布异常监控；行为特征窗口 ≥ 攻击者短期可操纵时长（避免被"养号"洗特征）；
- 回溯标签以事件系统为准，人工补标需引用工单号。

### 8.4 提示词扰动鲁棒（OpenJev 暴露的核心缺陷，探针路径强制项）

OpenJev 的 36 组扰动实测：仅**颠倒选项顺序**即有 27.8% 用例最高预测翻转，判定准则同义改写翻转 25.0%，混入无关上下文翻转 11.1%。对 M5 探针路径的强制对策：

1. **排列去偏**：每决策对候选做 k≥4 次随机排列取概率平均后再出参（延迟换稳定性，k 进版本四元组）；判别式主线因 softmax 头无位置概念，仅文本输入模板受此项约束；
2. **模板版本冻结**：提问模板、候选顺序表、措辞全部纳入 §9 版本管理——**对策略文本的"同义改写"视同模型升级**，必须重过 §11 门禁，防止运营人员改一句话就悄悄改变概率分布；
3. **扰动回归集**：顺序反转/同义改写/无关注入三类各 ≥36 用例进 §11.2，探针路径翻转率门槛 ≤10%（去偏后），未达标禁止进入 §6.2 阈值判定。

### 8.5 红队节奏

每迭代周期一次针对分类器的规避/投毒演练（模板见 §11.2 对抗回归集与 §8.4 扰动三类），结果进回归测试集。

---

## 9. 审计与可复现性

每条决策写不可变日志（append-only，WORM 存储）：

| 字段 | 说明 |
|---|---|
| decision_id, ts | 主键与时间 |
| fs_id, feature_digest | 特征快照引用 + 哈希校验 |
| model_id@version, encoder@version, threshold_set_id, schema_version | 版本四元组绑定 |
| raw_logits, calibrated_probs, chosen_action, confidence | 决策全链路 |
| hard_rules_matched, final_pdp_action, override_by, override_reason | 模型建议 vs 最终执行及人工覆盖 |
| cache_ttl, ttl_source | 缓存决策 |

- 版本四元组任一变更即新策略版本，需重过 §11 验收；
- 模型卡：数据窗口、ECE、对抗集成绩、已知局限随版本归档。

---

## 10. 监控与漂移

### 10.1 在线指标

score/概率分布（PSI）、逐头 ECE 代理、误拦率（申诉率代理）、**确认漏放率（事件回溯中"决策应为 deny/step_up 但实际 allow"占比——最高优先告警）**、灰区占比、fallback/超时计数、探针频率、推理延迟。

### 10.2 触发条件

PSI > 0.15 或 灰区占比周环比 ±50% 或 ECE 漂移超阈 → 触发重校准；确认漏放出现同类聚集 → 触发紧急阈值收紧（运营预案，不等重训）。

### 10.3 反馈环风险

用 PDP 结果弱监督 `action` 会让模型固化自身错误（错误 deny → 无后续观测）。缓解：灰区与 step_up 流量中恒定保留 ≥ 3% 的**探索性样本**做完整人工回溯标注，专用于无偏评测集（禁止混入训练）。

---

## 11. 评测与上线门禁

### 11.1 离线指标（测试集 = 最近 7 天真实分布）

| 指标 | v0 门槛 | v1 目标 |
|---|---|---|
| action macro-F1 | ≥ 0.80 | ≥ 0.88 |
| account_takeover / credential_stuffing AUROC | ≥ 0.90 | ≥ 0.94 |
| 全部头 ECE | ≤ 0.02 | ≤ 0.015 |
| risk band 相邻档混淆 | 相邻错误 ≤ 12%，跨档错误 = 0 | ≤ 6% |
| 误拦率（allow 应为实际被判 step_up/deny） | ≤ 2% 且申诉恢复 < 5min | ≤ 0.5% |
| 单前向延迟 P50 / P99 | < 10ms / < 200ms | 同左 |

### 11.2 对抗回归集

规避样本（阈值探针、养号漂移、模板注入）+ 投毒模拟，要求：规避成功率相对自然样本退化 ≤ 10%，注入文本不得使 P(allow) 提升 > 0.05。

**探针路径附加门禁（M5，对照 OpenJev 公开基线）**：选项顺序反转翻转率 ≤10%（OpenJev 未去偏基线 27.8%）、准则同义改写 ≤10%（基线 25.0%）、无关注入 ≤8%（基线 11.1%）；8 个 Noul 头对 TypeSafe 公开 102 行案例子集一致率 ≥85%（Qwen3.5-4B 零样本基线 84.5%，参照线非达标线）；校准前 ECE 记录进模型卡但不作门禁，校准后按 §11.1 执行。

### 11.3 上线流程（不可跳步）

1. **影子模式 ≥ 2 周**：模型输出仅记日志不执行，与规则 PDP 对比出差异报告；
2. 灰度 5%（仅 step_up 权限，不给 allow 裁量）→ 达标后放量；
3. 回滚开关：一键降级为纯规则 PDP（模型退出决策链），RTO < 60s。

---

## 12. 里程碑与分工建议

| 阶段 | 内容 | 出口条件 |
|---|---|---|
| M0 | 本契约冻结（schema/头清单/阈值设计/日志字段） | 安全 + 平台 + 业务三方评审签字 |
| M1 | Phase 0 规则上线、特征/快照管道、标注队列、数据飞轮 | §4.4 样本量达标 |
| M2 | LightGBM 基线 → 融合模型 v0，离线过 §11.1 | 过 v0 门槛 |
| M3 | PDP 集成、影子模式、对抗演练 | 差异报告 + §11.2 达标 |
| M4 | 灰度→全量、周度重校准、重训流水线 | 运行指标稳定 4 周 |
| M5 | 冷路径探针三方案离线对照：单步 Logits 探针（§5.2，免解码，预期 2.1–5.2×）vs 受限解码 JSON vs 判别头蒸馏（探针输出作蒸馏教师） | 过 §11.2 探针门禁 + §6.1 校准后 ECE 达标，产出与固定 schema 主线的收益/风险对比报告 |

人力画像：ML 工程 ×2（特征/训练/服务）、安全工程 ×1（规则/红队/事件回溯）、数据标注流程 ×0.5；主要成本在 M1 数据飞轮而非模型本身。

---

## 13. 外部对比基线（Jev / OpenJev 实测 vs 复现目标）

> 可视化版：同目录《Jev-OpenJev-模型能力对比.html》。口径警示见 13.2，引用时不得跨口径比较。

### 13.1 核心数据总表

| 维度 | TypeSafe Jev（官方宣称/第三方） | OpenJev（TheoLeeCJ 实测） | 本复现方案 v0.4（门禁目标，**未实测**） |
|---|---|---|---|
| 决策一致率 | 88.3%（官方 102 行公开案例）；**67.8%**（4 个真实业务流均值，第三方） | 40.7%（0.6B）/ 63.7%（2B）/ **84.5%（Qwen3.5-4B）**，同一 102 行子集 | 不跨域比分数；本域门禁见 §11.1（AUROC ≥0.90、macro-F1 ≥0.80 等） |
| 单次决策延迟 | 70–500 ms | 0.70 s（0.6B Q8）/ 1.51 s（2B Q4）/ 3.27 s（4B Q4），WebGPU 浏览器端 | 主线 P50 <10 ms / 控制面 P99 <200 ms；数据面缓存 <1 ms |
| 密集吞吐提速 | 宣称对自回归生成 40–200× | **5.21×**（21 问共享上下文，免解码 1.023s vs JSON 5.332s，0 token）；单决策 2.11–2.78× | 判别式一次前向出 8 头（结构性免解码）；M5 预期 2.1–5.2× |
| 概率校准 | RLCD 专项训练（宣称） | **无**——候选间条件概率（项目文档自认） | ECE ≤0.02 硬门禁 + §6.1 红线（未校准禁入阈值） |
| 扰动鲁棒 | 未公开 | 顺序反转翻转 **27.8%**、措辞改写 25.0%、无关注入 11.1%（各 36 用例） | 主线结构免疫（固定 schema）；探针去偏后翻转 ≤10%（§8.4/§11.2） |
| 成本 | 输入 $0.042/MTok，输出免费 | 本地开源免费（量化档 0.64–3.01 GB） | 本地免费 + 一次性训练/标注成本（M1 数据飞轮为主要投入） |
| 部署与自主性 | 闭源 API（含 OpenRouter/Cloudflare 渠道），权重不公开 | 纯前端 WebGPU + wllama 3.6.1，无后端/无遥测；≤20 选项、2048 token 截断 | 完全自托管（ONNX CPU / vLLM），版本四元组可审计 |
| Schema 弹性 | 任意类型化问题（官方原语内） | 自由提问 + 自由候选，零训练 | 主线固定 schema（冻结）；M5 探针保留自然语言弹性 |

### 13.2 口径与可信度声明

1. 88.3% vs 84.5% 同框**不严谨**：前者为 Jev 官方在其精选公开案例上的宣称值，后者是第三方在公开子集上对开源模型的实测——参照系，非头对头；
2. **67.8%（Jev 真实业务流）与 84.5%（探针公开子集）不在同一分布上**：真实业务先验更难，本方案以此约束预期（附录 A"天花板管理"）；
3. OpenJev 延迟为浏览器端数据（RTX 3090 + Chrome 152），服务端 vLLM/Ollama 单步打分接口下会显著改善，M5 实验须以我方硬件重测，不得沿用；
4. zhihz/openjev（另一同名开源实现）逐问重编码上下文，其延迟数据不可与 TheoLeeCJ 版互用；本表 OpenJev 均指 TheoLeeCJ 版；
5. 本方案列全部为**门禁目标值**，M2–M5 实测后回填并升版本；回填前禁止外引本页对比图。

### 13.3 综合评分雷达（0–10，换算规则见图表页脚注）

| 维度 | Jev | OpenJev-4B | 复现 v0.4 目标 |
|---|---|---|---|
| 决策准确率 | 8.5 | 8.0 | 7.5 |
| 概率校准 | 8.0 | 2.0 | 9.0 |
| 延迟性能 | 9.0 | 5.0 | 10.0 |
| 扰动鲁棒 | 3.0（未公开，保守计分） | 2.0 | 9.0 |
| 成本优势 | 8.0 | 10.0 | 8.0 |
| 自主开放 | 2.0 | 10.0 | 10.0 |
| 自然语言弹性 | 7.0 | 9.0 | 4.0 |
| 生产成熟度 | 7.0 | 3.0 | 5.0（设计期） |

---

## 附录 A：已知局限（诚实声明）

- 本规格复现的是 Jev 的**输出契约与机制**，不宣称达到其发布时的泛化水平与速度指标；后者含训练数据与 infra 红利，只能随迭代逼近。
- risk.score 为构造性代理标签，早期版本主要承载"排序"而非"绝对值"语义。
- 探索性样本（§10.3）在误拦代价高的业务里需按域调整比例。
- 中文/内部系统文本质量差的租户，`sensitive_policy_hit` 头会显著退化，需领域语料微调。
- **天花板管理**：第三方评测显示 Jev 自身在 4 个真实业务流的平均一致率也仅约 67.8%，而 4B 级零样本探针在 TypeSafe 公开案例子集上已能达 84.5%——"复刻 Jev 分数"不是目标，满足本域 §11.1 门禁才是；由此也不得以一致率提升为由收窄灰区与人工复核。
- 探针规模敏感性实测（0.6B 40.7% → 2B 63.7% → 4B 84.5%）说明**小模型免训练探针没有免费午餐**：省掉训练数据的同时用参数量偿还，M5 若降级到 2B 必须配 LoRA 微调。
